#!/usr/bin/env perl
use warnings;
use strict;
use FindBin;
use File::Spec;
# add current directory on the fly
use lib File::Spec->catfile($FindBin::Bin);
# import all
use branchLib;
use controller;
use pathLib;
use commitTreeLib;
use fileLib;

use textLib;
use typeLib;
# this script for testing

# my @arr = get_commit_link(5);
# print "commit link ", @arr;
# # my  @ancestors = get_ancestor(5,6);
# # print "ancestors ",@ancestors;
#
# my %commit_hash= get_commit_hash();
# if (is_ancestor_of(5,2,%commit_hash)){
#   print "yes";
# }



# sub get_diff {
#   # my %merged_diff;
#   # return null
#   return ;
# }
#
# my %hash = get_diff();
#
# if (! keys %hash) {
#   dd_err("hash is empty ")
# }
